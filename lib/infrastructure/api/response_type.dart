enum ResponseType {
  SUCCESS,
  NETWORK_ERROR,
  VALIDATION_ERROR,
  CLIENT_ERROR,
  SERVER_ERROR
}


enum RequestState{initial, success, failureValidation, loadingData, serverError, networkError}


